# Конфигурационные файлы

В этой папке хранятся **настройки проекта**:

- `.yaml` файлы с параметрами:
  - гиперпараметры модели;
  - схемы датасета;
- шаблоны файлов окружения:
  - `.env.example` — список переменных окружения без реальных секретов.
